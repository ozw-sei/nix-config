# Copy this file to ~/.config/git/config.local and replace the values below.
# Remove the leading "#" from each setting after editing.
#
# [user]
#   name = Your Name
#   email = your-email@example.com
